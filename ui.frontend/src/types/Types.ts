export type GitUser = {
    html_url: string;
    avatar_url: string;
    login: string;
    id: number;
    url: string;
  };

  export type GitUserInfo = {
    name: string;
    bio: string;
    repos_url: string;
  };